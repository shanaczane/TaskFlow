// src/components/RightSidebar.jsx
function RightSidebar({ isDarkMode }) {
  return (
    <div
      style={{
        width: "300px",
        padding: "20px",
        borderLeft: "1px solid #ddd",
        backgroundColor: isDarkMode ? "#1e1e1e" : "#fff",
      }}
    >
      {/* Empty - add details later */}
    </div>
  );
}

export default RightSidebar;